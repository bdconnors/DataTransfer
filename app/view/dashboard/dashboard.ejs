<%- include('../include/head.ejs',{title:'File System Home',scripts:false})%>
<body>
<%- include('../include/menu.ejs',{system:system}) %>
<% var user = system.getUserInfo()%>

<div id="welcomeHeader" style="padding:35px 50px; width:100%;">
    <h1> <i class="fa fa-user-circle-o " ></i> Welcome <%= user.firstname %></h1>
</div>
<br>

    <div class="table-responsive">
        <table class="table-borderless">
            <%var columnSize = 3%>
            <% var projects = user.getProjects()%>
            <% var totalCells = projects.length%>
            <% var fullRows = Math.floor(projects.length/columnSize)%>
            <% var partialRowCount = projects.length % columnSize%>
            <% var rows = fullRows+1%>
            <% var cellCount = 0%>

            <%for(var i = 0; i < rows; i++){

            %>
                <tr>
                <%var cellsInRow = 3%>
                <%if(i === fullRows){
                    cellsInRow = partialRowCount;
                }%>
                <%for(var j = 0; j < cellsInRow; j++){

                %>
                    <td>
                        <div class="dropdown">
                            <button style = "background-color:transparent; font-size:16px" class="btn btn-lg dropdown-toggle" type="button" id="projSettings" data-toggle="dropdown" aria-haspopup ="true" aria-expanded="false">
                                <i class="fa fa-cog"></i> Options
                            </button>
                            <div class="dropdown-menu" aria-labelledby="projSettings">
                                <form action="/projects/project/<%= projects[cellCount].getId()%>" method="get">
                                    <button class="dropdown-item" type="submit"><i class="fa fa-eye"></i> View</button>
                                </form>
                                <%if(user.admin){%>
                                    <form action="/projects/project/<%= projects[cellCount].id%>/rename%>" method="get">
                                        <button type="submit" class="dropdown-item"><i class="fa fa-pencil"></i> Rename</button>
                                    </form>
                                    <form action="/projects/project/<%= projects[cellCount].id%>/delete%>" method="post">
                                        <button type="submit" class="dropdown-item"><i class="fa fa-minus-square-o"></i> Delete</button>
                                    </form>
                                        <div class="dropdown-divider"></div>
                                    <form action="/projects/project/<%= projects[cellCount].id%>/permissions%>" method="get">
                                        <button type="submit" class="dropdown-item"><i class="fa fa-user"></i> Permissions</button>
                                    </form>
                                <%}%>
                            </div>
                        </div>
                        <a style = ";text-decoration: none; color:#343a40;" href="/projects/project/<%= projects[cellCount].getId()%>">
                            <i class="fa fa-archive fa-3x"></i><br><span style="font-size:14px;"><%= projects[cellCount].getName()%></span>
                        </a>
                    <%cellCount++%>
                <%}%>
                </tr>
            <%}%>
        </table>
    </div>
<%- include('../include/foot.ejs')%>
